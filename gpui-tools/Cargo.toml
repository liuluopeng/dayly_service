[package]
name = "gpui-tools"
version = "0.1.0"
edition = "2024"

[dependencies]
gpui = { version = "0.2", default-features = false, features = ["font-kit", "macos-blade"] }
common = { path = "../common" }
